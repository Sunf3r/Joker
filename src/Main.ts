/** Main.ts
 * Você deve compilar esse arquivo para EXE e manter
 * o Updater como .TS na mesma pasta.
 *
 * Isso serve como runtime para executar os arquivos .ts
 * Assim, o updater também pode ser atualizado sem
 * precisar baixar um EXE novamente
 */

import('./Updater.ts');
